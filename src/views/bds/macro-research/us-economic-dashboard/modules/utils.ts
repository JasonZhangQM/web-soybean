/**
 * 美国宏观看板 - 通用工具函数
 * 跨目录复用 macro-research/_shared 的通用函数，避免代码重复
 */
export { normalize, forwardFill, getLatest, getSeries } from '../../_shared/utils';
export type { DerivedItem } from '../../_shared/utils';
